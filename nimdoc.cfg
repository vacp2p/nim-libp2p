doc.item = """
<div id="$itemSymOrID" class="tohide">
<script type="text/javascript">
if (typeof main2 === "undefined") {
	function main2() {
		var toHide = document.getElementsByClassName("tohide");
		console.log(toHide);
		for (var i = 0; i < toHide.length; i++) {
			var pragmas = toHide[i].querySelectorAll("span.pragma span.Identifier");
			var public = false;
			for (var z = 0; z < pragmas.length; z++) {
				if (pragmas[z].innerText == "public") {
					public = true;
					break;
				}
			}
			if (public == false) {
				toHide[i].style.display = "none";
				links = document.querySelectorAll("a[href='#" + encodeURIComponent(toHide[i].id) + "']");
				for (var y = 0; y < links.length; y++) {
					links[y].parentNode.style.display = "none";
				}
			}
		}

		document.getElementById("tocRoot").innerHTML += "<button onclick='showPrivateProcs()'>Show internal procedures</button>";
	}

	function showPrivateProcs() {
		var toHide = document.getElementsByClassName("tohide");
		for (var i = 0; i < toHide.length; i++) {
			toHide[i].style.display = "";
		}
		toHide = document.links;
		for (var i = 0; i < toHide.length; i++) {
			toHide[i].parentNode.style.display = "";
		}
	}
	window.addEventListener("DOMContentLoaded", main2);
}
</script>
<dt><pre>$header</pre></dt>
<dd>
$deprecationMsg
$desc
$seeSrc
</dd>
</div>
"""
