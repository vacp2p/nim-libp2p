name: Test AutoTLS Docker Integration

on:
  pull_request:
  merge_group:
  push:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  autotls-docker-integration:
    # No matrix: the test shares a network namespace with the containers, which only
    # works on Linux.
    name: Issue a certificate against a local ACME server and broker
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: true

      - name: Issue a certificate
        run: make test_autotls_docker_integration
