name: Daily Nim Devel
on:
#  schedule:
#    - cron: "30 6 * * *"
  pull_request:
  workflow_dispatch:

jobs:
  call-multi-nim-common:
    uses: ./.github/workflows/multi_nim_common.yml
    with:
      nim-branch: "['devel']"
      cpu: "['amd64']"
